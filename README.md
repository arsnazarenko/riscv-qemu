# QEMU RISC-V

```sh
./build_opensbi.sh # for custom build path use: OPENSBI_OUT=./my/dir ./build_opensbi.sh

./build_linux.sh # for custom build path use: LINUX_OUT=./my/dir ./build_linux.sh

./build_rootfs.sh # for custom build path use: BUILDROOT_OUT=./my/dir ./build_rootfs.sh

```

